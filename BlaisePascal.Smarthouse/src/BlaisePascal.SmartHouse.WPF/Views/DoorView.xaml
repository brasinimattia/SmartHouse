<UserControl x:Class="BlaisePascal.SmartHouse.WPF.Views.DoorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1280">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F0F4F8" Offset="0.0" />
                <GradientStop Color="#D9E2EC" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <!-- LISTA PORTE (Sinistra) -->
            <DockPanel Grid.Column="0">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10,0,0,30">
                    <materialDesign:PackIcon Kind="Door" Width="35" Height="35" VerticalAlignment="Center" Foreground="#5C6BC0"/>
                    <TextBlock Text="Door Manager" Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                               FontWeight="Bold" Foreground="#334E68" Margin="15,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="DoorList" Background="Transparent" BorderThickness="0" SelectionChanged="DoorList_SelectionChanged">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Left" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="220" Height="160" Margin="10" Padding="20" CornerRadius="24" BorderThickness="2">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#A6FFFFFF"/>
                                            <Setter Property="BorderBrush" Value="#E6FFFFFF"/>
                                            <Style.Triggers>
                                                <!-- Effetto se la porta è bloccata -->
                                                <DataTrigger Binding="{Binding LockingStatus}" Value="Locked">
                                                    <Setter Property="BorderBrush" Value="#D32F2F" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="{Binding DoorStatus, Converter={StaticResource DoorIconConverter}}" Width="28" Height="28" Foreground="#5C6BC0"/>
                                            <materialDesign:PackIcon Kind="Lock" Width="20" Height="20" Margin="5,0,0,0" 
                                                                     Visibility="{Binding LockingStatus, Converter={StaticResource VisibilityConverter}}" Foreground="#D32F2F"/>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" Foreground="#37474F" VerticalAlignment="Center"/>

                                        <TextBlock Grid.Row="2" Text="{Binding DoorStatus}" FontSize="12" FontWeight="Bold" Foreground="#607D8B"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </DockPanel>

            <!-- GESTIONE (Destra) -->
            <Border Grid.Column="1" Background="#A6FFFFFF" CornerRadius="28" Padding="30" Margin="15,0,0,0">
                <StackPanel>
                    <TextBlock Text="Management" FontWeight="Bold" FontSize="20" Margin="0,0,0,20"/>
                    
                   
                    <TextBox x:Name="NewDoorNameTextBox" materialDesign:HintAssist.Hint="Door Name" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15"/>
                    <Button Content="ADD DOOR" Click="AddDoor_Click" Background="#5C6BC0" materialDesign:ButtonAssist.CornerRadius="24" Height="48"/>

                    <Separator Margin="0,30" Background="#20607D8B"/>

                    <!-- Controlli Apertura -->
                    <UniformGrid Columns="2" Margin="0,0,0,15">
                        <Button Content="OPEN" Click="Open_Click" Margin="0,0,5,0" Background="#5C6BC0"/>
                        <Button Content="CLOSE" Click="Close_Click" Margin="5,0,0,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    </UniformGrid>

                    <Separator Margin="0,15" Background="#10607D8B"/>

                    <!-- Controlli Sicurezza -->
                    <PasswordBox x:Name="DoorPasswordBox" materialDesign:HintAssist.Hint="Security Key" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,15"/>
                    
                    <UniformGrid Columns="2" Margin="0,0,0,10">
                        <Button Content="LOCK" Click="Lock_Click" Margin="0,0,5,0" Background="#37474F"/>
                        <Button Content="UNLOCK" Click="Unlock_Click" Margin="5,0,0,0" Background="#78909C"/>
                    </UniformGrid>

                    <Button Content="SET NEW PASSWORD" Click="SetPassword_Click" Style="{StaticResource MaterialDesignFlatMidBgButton}" Margin="0,10"/>
                    
                    <Button Click="Remove_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#D32F2F" BorderBrush="#FFCDD2" Margin="0,20,0,0">
                        <materialDesign:PackIcon Kind="Delete"/>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>